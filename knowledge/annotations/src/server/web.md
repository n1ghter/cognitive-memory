```markdown
# Web Server Implementation

The web server exposes the MCP (Model-Driven Configuration) tools and interacts with SQLite tables through the `DatabaseManager` instance. It serves a graph API endpoint that fetches nodes and links from memory tables in both local and global databases.
```