**ui/vitest.config.ts**

This configuration file defines the settings for Vitest testing framework used in the project. It enables React support and specifies a custom `jsdom` environment, ensuring consistent test results across the application.

### MCP Exposures:
None explicitly listed.

### SQLite Interactions: None explicitly listed.

### Architectural Purpose:
Enables Vitest testing with React support, allowing for robust unit testing of the application's UI components.