**Tests for Database Manager and Utils**

This file tests the functionality of `DatabaseManager` and its utility functions, ensuring they correctly generate UUIDs, normalize record IDs, initialize in-memory databases, and handle fallback cases.

*   MCP tools used:
    *   `DatabaseManager`
*   Interacts with SQLite tables:
    *   `sqlite_master` table