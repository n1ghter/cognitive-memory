```markdown
**Semantic Summary**
=====================

This test suite validates the functionality of the `executeMemoryDelete` tool, ensuring correct soft and hard deletion of memory entries in a simulated database.

**Interaction with SQLite Tables**
--------------------------------

* `DatabaseManager`
* `memory` table

**MCP Tools Exposed**
--------------------

* `executeMemoryDelete`
```