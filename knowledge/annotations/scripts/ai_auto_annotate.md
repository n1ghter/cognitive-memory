### AI Auto-Annotator Script
#### Purpose:
Automate the generation of JSDoc comments and semantic annotations for TypeScript files using an AI-powered tool.

#### Notes:
* Interacts with `OllamaClient` to generate code and annotation responses.
* Exposes MCP tools (git) via system commands.
* Lists explicitly SQLite interactions.